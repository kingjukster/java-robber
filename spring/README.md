# Cops vs Robbers Spring Boot Backend

This module contains a Spring Boot implementation of the Cops vs Robbers backend.

## Build & Run

```
mvn spring-boot:run
```

Configure database connection in `src/main/resources/application.properties`.
